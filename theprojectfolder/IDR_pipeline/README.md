## Setup Instructions

1. Copy the setup template:

```bash
   cp setup.sh.example setup.sh
```

2. The script will automatically fetch your AWS Account ID

3. Load environment variables:

```bash
   source setup.sh
```

**Note:** `setup.sh` is gitignored to protect your AWS Account ID
